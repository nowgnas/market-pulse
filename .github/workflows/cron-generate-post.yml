name: Generate Market Post

on:
  schedule:
    # Backup schedule: KST 08:10 = UTC 23:10 (previous day)
    - cron: '10 23 * * *'
    # Backup schedule: KST 12:10 = UTC 03:10
    - cron: '10 3 * * *'
    # Backup schedule: KST 18:10 = UTC 09:10
    - cron: '10 9 * * *'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Call generate-post API
        run: |
          response=$(curl -s -w "\n%{http_code}" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.SITE_URL }}/api/cron/generate-post")

          http_code=$(echo "$response" | tail -1)
          body=$(echo "$response" | head -n -1)

          echo "Status: $http_code"
          echo "Response: $body"

          if [ "$http_code" != "200" ]; then
            echo "::error::API call failed with status $http_code"
            exit 1
          fi
